# --- App / API
fastapi
uvicorn[standard]
sqlmodel
psycopg2-binary
python-dotenv
openai

# --- PDF / OCR
pymupdf          # import fitz
pytesseract      # requer tesseract instalado no SO (veja observação abaixo)
Pillow

# --- Vetores / IA
qdrant-client
sentence-transformers
scikit-learn
numpy

# --- Validações / Auth / Upload
email-validator
PyJWT
passlib[bcrypt]
python-multipart

# --- Torch (CPU-only) para evitar CUDA no Render
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.2+cpu


# Autenticação (pinnar versões compatíveis)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
